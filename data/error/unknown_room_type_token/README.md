# error/unknown_room_type_token —— 类型词无法识别（容错归一）

**类别**：错误数据 · 教室类型 "Computer"、所需类型 "机房" 均不在枚举内

## 预期
导入成功并**静默归一**：教室 "Computer1" 被当 Norm；
C101 所需的 "机房" 被当 Any（不限）、C102 需要 Lab 可正常用 Lab1。
展示解析对未知类型词的宽容（与文档口径一致）。
