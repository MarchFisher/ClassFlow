# error/inconsistent_course_columns —— 同 courseId 内嵌课程字段打架

**类别**：错误数据 · 两班同属 C01，但课程名/学分/每周次数/学院各不相同

## 预期
导入成功；课程以**首行字段建档**（“高等数学,4,2,2,理学院”），后续行的内嵌课程列被忽略
——数据事实不一致却静默取首行。验证课程去重/权威口径（v4.1 R4 语义的镜像面）。
